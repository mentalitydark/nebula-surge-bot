services:
  bot:
    tty: true
    build:
      context: .
      target: development
    restart: unless-stopped
    environment:
      - FORCE_COLOR=1
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
